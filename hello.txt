hello im superman

==================================================
TODO: Server-Side Upload API for Admin Images
==================================================
Problem: RLS blocks direct client uploads via anon key
Solution: Upload through server API using service_role key (bypasses RLS)

Steps:
[ ] 1. Add SUPABASE_SERVICE_ROLE_KEY to .env (get from Supabase Dashboard > Settings > API Keys)
[ ] 2. Create lib/supabase-admin.ts — server-only Supabase client using service_role key
       (import { createClient } from '@supabase/supabase-js'; const supabaseAdmin = createClient(url, serviceRoleKey))
[ ] 3. Create app/api/admin/upload/route.ts — POST endpoint that:
       - Receives FormData (file)
       - Validates: file type (image/*), max size (5MB)
       - Uploads to tadorado-assets/products/ using supabaseAdmin
       - Returns JSON { url: "..." }
[ ] 4. Update components/admin/ImageUpload.tsx — replace direct supabase.storage.upload() with:
       - POST file to /api/admin/upload
       - Get URL from response
[ ] 5. Add SUPABASE_SERVICE_ROLE_KEY to Vercel env vars for production
[ ] 6. Delete scripts/check-bucket.ts and scripts/list-buckets.ts (no longer needed)
